<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.company.p2onboarding">

    <uses-sdk tools:overrideLibrary="com.google.zxing.client.android" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".cronologia"
            android:exported="true" />
        <activity android:name=".autoridadesActivity" />
        <activity android:name=".cultura_activity" />
        <activity android:name=".sedes_activity" />
        <activity android:name=".ilustres_activity" />
        <activity android:name=".menuGeneral"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".MapaUnmsm"
            android:exported="true" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCDhayUru4wrFsGNTAFUjRLStzPcAbTs74" />

        <activity android:name=".AcercaDe" />
        <activity android:name=".clinicaUniversitariaUnmsm" />
        <activity android:name=".plazaFrayTomasUnmsm" />
        <activity android:name=".huacaUnmsm" />
        <activity android:name=".estadioUnmsm" />
        <activity android:name=".bibliotecaUnmsm" />
        <activity android:name=".fisiUnmsm" />
        <activity android:name=".ResetPasswordActivity" />
        <activity android:name=".HomeAppActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".RegistrarseActivity" />
        <activity android:name=".IniciarSesionActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
    </application>

</manifest>